# Инструкция по применению изменений

## 🎯 Что было сделано

1. ✅ **Генерация уникального UID** - формат `AZ-{ГОД}-{ID}`
2. ✅ **Динамическая аватарка** - загружается из Google профиля
3. ✅ **Реальные новости** - загружаются с сервера

## 📋 Шаги для применения

### 1. Миграция базы данных

Запустите скрипт миграции для добавления UID существующим пользователям:

```bash
node server/scripts/migrate-add-uid.js
```

Этот скрипт:
- Добавит колонки `avatar` и `uid` в таблицу `users` (если их нет)
- Сгенерирует UID для всех существующих пользователей
- Выведет список обновленных пользователей

### 2. Перезапуск сервера

Если сервер уже запущен, перезапустите его:

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова
npm start
```

Или если используете Render.com - сервер перезапустится автоматически после push.

### 3. Проверка изменений

#### Проверка на сервере:

1. Откройте браузер и перейдите на сайт
2. Войдите через Google
3. Проверьте, что:
   - Аватарка загрузилась из Google
   - UID отображается в формате `AZ-YYYY-XXX`
   - Новости загружаются с сервера

#### Проверка в лаунчере:

1. Запустите лаунчер
2. Войдите через Google
3. Перейдите в профиль и проверьте:
   - Аватарка отображается
   - UID в правильном формате
4. Перейдите в новости и проверьте:
   - Новости загружаются с сервера
   - Отображаются только новости типа "launcher"

### 4. Создание новостей (опционально)

Если в базе данных нет новостей, создайте их через API:

```bash
curl -X POST https://oneshakedown.onrender.com/api/news \
  -H "Content-Type: application/json" \
  -d '{
    "title": "ShakeDown Client 3.2.1 Release",
    "content": "Добавлена генерация UID, динамическая аватарка и реальные новости!",
    "author": "Admin",
    "type": "launcher"
  }'
```

## 🔍 Проверка работы

### Проверка UID:

```sql
SELECT id, username, uid, registered_at FROM users;
```

Должны увидеть UID в формате `AZ-2025-001`, `AZ-2024-042` и т.д.

### Проверка аватарок:

```sql
SELECT id, username, avatar FROM users WHERE avatar IS NOT NULL;
```

Должны увидеть URL аватарок из Google.

### Проверка новостей:

```bash
curl https://oneshakedown.onrender.com/api/news
```

Должны получить список новостей в JSON формате.

## ⚠️ Возможные проблемы

### Проблема: UID не генерируется для новых пользователей

**Решение**: Проверьте, что сервер перезапущен после изменений в `server/index.js`

### Проблема: Аватарка не загружается

**Решение**: 
1. Проверьте, что Google OAuth настроен правильно
2. Убедитесь, что в Google профиле есть фото
3. Проверьте логи сервера на наличие ошибок

### Проблема: Новости не загружаются в лаунчере

**Решение**:
1. Проверьте, что API URL правильный: `https://oneshakedown.onrender.com`
2. Убедитесь, что в базе есть новости с типом `launcher`
3. Проверьте CORS настройки на сервере

## 📝 Дополнительная информация

- Полный changelog: `CHANGELOG-UID-AVATAR-NEWS.md`
- Изменения в лаунчере: `Launcher/ИЗМЕНЕНИЯ.txt`
- Скрипт миграции: `server/scripts/migrate-add-uid.js`

## ✅ Готово!

После выполнения всех шагов:
- UID будет генерироваться автоматически для новых пользователей
- Аватарки будут загружаться из Google профиля
- Новости будут загружаться с сервера в реальном времени
