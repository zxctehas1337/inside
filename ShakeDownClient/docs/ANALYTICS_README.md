# 📊 Система аналитики ShakeDown

## Описание

Добавлена полноценная система аналитики для отслеживания активности пользователей на сайте.

## Возможности

### 📈 Отслеживаемые метрики:
- **Посещения страниц** - количество просмотров каждой страницы
- **Уникальные пользователи** - количество уникальных посетителей
- **Клики по элементам** - отслеживание кликов по кнопкам и ссылкам
- **Время на сайте** - среднее время, проведенное пользователями
- **Активность по часам** - график активности за последние 24 часа
- **Популярные страницы** - топ самых посещаемых страниц

### 📊 Визуализация:
- Графики посещений за последние 7 дней
- Почасовая активность за 24 часа
- Топ популярных страниц
- Топ кликов по элементам
- Карточки с основными метриками

## Структура

### Backend (server/index.js)
```javascript
// Таблица analytics в PostgreSQL
CREATE TABLE analytics (
  id SERIAL PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  event_type VARCHAR(100) NOT NULL,
  page VARCHAR(255),
  data JSONB,
  timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)

// API endpoints:
POST /api/analytics - Запись события
GET /api/analytics/stats - Получение статистики
```

### Frontend

#### Компоненты:
- `src/components/AnalyticsPanel.tsx` - Панель с графиками и статистикой
- `src/styles/AnalyticsPanel.css` - Стили для панели аналитики

#### Утилиты:
- `src/utils/analytics.ts` - Функции для отслеживания событий
- `src/utils/api.ts` - API функции для работы с аналитикой

## Использование

### Автоматическое отслеживание

Аналитика автоматически отслеживает:
- Посещения страниц при загрузке
- Клики по кнопкам "Скачать", "Купить премиум", "Купить альфа"
- Переходы по ссылкам авторизации
- Время, проведенное на странице

### Ручное отслеживание

```typescript
import { trackPageView, trackButtonClick, trackLinkClick } from '../utils/analytics'

// Отслеживание просмотра страницы
trackPageView('/custom-page')

// Отслеживание клика по кнопке
trackButtonClick('custom_button_name')

// Отслеживание клика по ссылке
trackLinkClick('custom_link_name')
```

### Просмотр статистики

Статистика доступна в админ-панели:
1. Войдите как администратор
2. Перейдите на вкладку "Аналитика"
3. Просмотрите графики и метрики

## Интеграция

### Страницы с аналитикой:
- ✅ HomePage - главная страница
- ✅ AuthPage - страница авторизации
- ✅ DashboardPage - личный кабинет
- ✅ NewsPage - страница новостей
- ✅ AdminPage - админ-панель (просмотр статистики)

### Отслеживаемые события:
- `page_view` - просмотр страницы
- `click` - клик по элементу
- `form_submit` - отправка формы
- `page_exit` - выход со страницы
- `error` - ошибка

## Обновление данных

- Статистика обновляется автоматически каждые 30 секунд
- Данные хранятся в PostgreSQL
- Индексы созданы для быстрого поиска

## Безопасность

- Аналитика работает как для авторизованных, так и для анонимных пользователей
- Для анонимных пользователей `user_id` = NULL
- Все данные хранятся на сервере
- Нет использования сторонних сервисов аналитики

## Производительность

- Запросы к API выполняются асинхронно
- Не блокируют основной поток
- Минимальное влияние на производительность сайта
- Оптимизированные SQL запросы с индексами
