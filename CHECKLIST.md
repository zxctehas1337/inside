# Чеклист перед деплоем

## ✅ Что уже сделано:

- [x] Установлен `jsonwebtoken` (npm install выполнен)
- [x] Обновлен `server/index.js` с JWT токенами
- [x] Создана резервная копия `server/index.backup.js`
- [x] Обновлен `src/utils/database.ts` для сохранения токенов в localStorage
- [x] Обновлен `src/pages/AuthPage.tsx` для работы с токенами
- [x] Обновлен `Launcher/src/App.tsx` для работы с токенами
- [x] Добавлен `JWT_SECRET` в `.env` файл
- [x] Проверены все файлы на ошибки (getDiagnostics)

## 📋 Что нужно сделать:

### 1. Добавить JWT_SECRET на Render.com
- [ ] Зайти на https://dashboard.render.com/
- [ ] Выбрать сервис "oneshakedown"
- [ ] Перейти в Environment
- [ ] Добавить переменную:
  - Key: `JWT_SECRET`
  - Value: `KOTAKBAS3991-JWT-SECRET-KEY-2024`
- [ ] Нажать Save Changes

### 2. Задеплоить на Render
- [ ] Выполнить команды:
  ```bash
  git add .
  git commit -m "Миграция на JWT токены для устойчивости к передеплою"
  git push origin main
  ```
- [ ] Дождаться завершения деплоя на Render (2-5 минут)

### 3. Проверить работу
- [ ] Открыть https://oneshakedown.onrender.com
- [ ] Войти через Google или Yandex
- [ ] Проверить, что вход работает
- [ ] Закрыть браузер и открыть снова
- [ ] Убедиться, что остались авторизованными
- [ ] (Опционально) Сделать Manual Deploy на Render и проверить, что не вышло из аккаунта

### 4. Проверить лаунчер
- [ ] Запустить лаунчер
- [ ] Войти через Google или Yandex
- [ ] Закрыть и открыть лаунчер
- [ ] Убедиться, что остались авторизованными

## 🎯 Ожидаемый результат:

После деплоя пользователи:
- ✅ Смогут войти через Google/Yandex
- ✅ Останутся авторизованными после закрытия браузера
- ✅ НЕ ВЫЙДУТ из аккаунта при передеплое сервера
- ✅ Токены будут действительны 30 дней

## ⚠️ Важно:

Существующие пользователи должны будут войти заново **один раз** после обновления.

## 🔄 Откат (если что-то пошло не так):

```bash
cp server/index.backup.js server/index.js
git add server/index.js
git commit -m "Откат к версии с сессиями"
git push origin main
```

## 📞 Поддержка:

Если возникли проблемы:
1. Проверьте логи на Render.com
2. Убедитесь, что JWT_SECRET добавлен
3. Проверьте консоль браузера (F12) на ошибки
