# 📧 Настройка Email Верификации

## Что было добавлено

### 1. Серверная часть (server/index.js)
- ✅ Генерация 6-значного кода подтверждения
- ✅ Отправка кода на email через Gmail SMTP
- ✅ Эндпоинт `/api/auth/verify-code` для проверки кода
- ✅ Эндпоинт `/api/auth/resend-code` для повторной отправки
- ✅ Срок действия кода: 10 минут
- ✅ Обновление структуры БД с полями `verification_code` и `verification_code_expires`

### 2. Клиентская часть (AuthPage.tsx)
- ✅ Модальное окно для ввода 6-значного кода
- ✅ Автоматический переход между полями ввода
- ✅ Кнопка повторной отправки кода
- ✅ Валидация кода перед отправкой
- ✅ Красивый дизайн с анимациями

### 3. API функции (api.ts)
- ✅ `verifyEmailCode(userId, code)` - проверка кода
- ✅ `resendVerificationCode(userId)` - повторная отправка

## Как это работает

### Процесс регистрации:

1. **Пользователь заполняет форму регистрации**
   - Вводит username, email, пароль
   - Нажимает "Зарегистрироваться"

2. **Сервер создает аккаунт**
   - Генерирует 6-значный код (например: 123456)
   - Сохраняет код в БД с временем истечения (10 минут)
   - Отправляет код на email пользователя

3. **Пользователь получает письмо**
   - Красивое HTML письмо с кодом
   - Код выделен крупным шрифтом
   - Указано время действия (10 минут)

4. **Пользователь вводит код**
   - Открывается модальное окно
   - 6 полей для ввода цифр
   - Автоматический переход между полями

5. **Проверка кода**
   - Сервер проверяет код и срок действия
   - При успехе: `email_verified = true`
   - Пользователь перенаправляется в dashboard

## Тестирование

### 1. Тест отправки кода
```bash
npm run test:code
```
Отправит тестовое письмо с кодом на ваш email.

### 2. Полный тест регистрации
```bash
npm run test:registration
```

### 3. Ручное тестирование
1. Запустите сервер: `npm start`
2. Откройте http://localhost:8080/auth
3. Зарегистрируйте нового пользователя
4. Проверьте почту
5. Введите полученный код

## Структура БД

```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR(255) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password TEXT NOT NULL,
  email_verified BOOLEAN DEFAULT false,
  verification_code VARCHAR(6),
  verification_code_expires TIMESTAMP,
  ...
);
```

## Настройки SMTP

В файле `.env`:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=YourApp <your-email@gmail.com>
```

## API Эндпоинты

### POST /api/auth/register
Регистрация с отправкой кода
```json
{
  "username": "user",
  "email": "user@example.com",
  "password": "password123"
}
```

Ответ:
```json
{
  "success": true,
  "message": "Код подтверждения отправлен на email",
  "requiresVerification": true,
  "data": { "id": 1, "username": "user", ... }
}
```

### POST /api/auth/verify-code
Проверка кода
```json
{
  "userId": 1,
  "code": "123456"
}
```

Ответ:
```json
{
  "success": true,
  "message": "Email успешно подтвержден!"
}
```

### POST /api/auth/resend-code
Повторная отправка кода
```json
{
  "userId": 1
}
```

Ответ:
```json
{
  "success": true,
  "message": "Новый код отправлен на email"
}
```

## Безопасность

- ✅ Код действителен только 10 минут
- ✅ Код удаляется после успешной верификации
- ✅ Можно запросить новый код
- ✅ Проверка на существование пользователя
- ✅ Проверка срока действия кода

## Возможные улучшения

1. **Ограничение попыток**
   - Максимум 3 попытки ввода кода
   - Блокировка на 15 минут после превышения

2. **Уведомления**
   - SMS верификация как альтернатива
   - Push уведомления

3. **Аналитика**
   - Логирование попыток верификации
   - Статистика успешных/неуспешных попыток

4. **UI улучшения**
   - Таймер обратного отсчета
   - Индикатор прогресса
   - Анимация успешной верификации
