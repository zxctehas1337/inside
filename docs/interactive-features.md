# Интерактивные возможности главной страницы

## Обзор

Главная страница ShakeDown Client содержит интерактивное превью лаунчера с множеством удивительных эффектов, которые делают взаимодействие с сайтом запоминающимся.

## Реализованные эффекты

### 1. 🎯 Jelly Drag Effect (Эффект желе при перетаскивании)

**Как работает:**
- Когда превью лаунчера свёрнуто, его можно перетаскивать мышью
- При перетаскивании применяется физика "желе" с инерцией и затуханием
- После отпускания окно плавно возвращается в исходное положение с пружинящим эффектом

**Технические детали:**
- Использует `requestAnimationFrame` для плавной анимации
- Коэффициент затухания: 0.92 (damping)
- Коэффициент возврата: 0.85 (spring back)
- Добавляет небольшое вращение в зависимости от скорости движения

### 2. ✨ Particle System (Система частиц)

**Как работает:**
- При клике на свёрнутое окно создаётся 12 частиц
- Частицы разлетаются радиально во все стороны
- Применяется гравитация для реалистичного падения
- Частицы постепенно исчезают (fade out)

**Параметры:**
- Количество частиц: 12
- Время жизни: 60 кадров (~1 секунда)
- Гравитация: 0.5 пикселей/кадр²
- Начальная скорость: 2-5 пикселей/кадр

### 3. 🌊 Ripple Effect (Волновой эффект)

**Как работает:**
- При клике на развёрнутое окно создаётся волна
- Волна расширяется от точки клика
- Плавное исчезновение с увеличением размера

**Параметры:**
- Начальный размер: 20px
- Конечный размер: 200px
- Длительность: 1 секунда
- Цвет: градиент от фиолетового к прозрачному

### 4. 🎨 3D Hover Tilt (3D наклон при наведении)

**Как работает:**
- При наведении мыши окно наклоняется в 3D пространстве
- Угол наклона зависит от позиции курсора
- Создаёт эффект глубины и объёма

**Параметры:**
- Максимальный угол наклона: ±10 градусов
- Перспектива: 1000px
- Плавный переход с cubic-bezier(0.34, 1.56, 0.64, 1)

### 5. 💫 Shine Sweep (Блик света)

**Как работает:**
- При наведении по окну проходит световой блик
- Создаёт эффект отражения света на глянцевой поверхности
- Бесконечная анимация с интервалом 3 секунды

**Параметры:**
- Угол: 45 градусов
- Цвет: белый с прозрачностью 10-20%
- Длительность цикла: 3 секунды

### 6. ⚡ Pulse Glow (Пульсирующее свечение)

**Как работает:**
- Свёрнутое окно пульсирует мягким свечением
- Привлекает внимание к интерактивному элементу
- Плавное изменение интенсивности

**Параметры:**
- Цвет свечения: фиолетовый (#8A4BFF)
- Минимальная интенсивность: 0.3
- Максимальная интенсивность: 0.6
- Период: 2 секунды

### 7. 🎪 Jelly Wobble (Желеобразное колебание)

**Как работает:**
- Во время перетаскивания окно "дрожит" как желе
- Создаёт ощущение мягкости и эластичности
- Синхронизировано с движением

**Параметры:**
- Амплитуда: ±5% от размера
- Частота: 0.5 секунды на цикл
- 4 фазы колебания

## Использование

### Базовая интеграция

```tsx
import InteractiveLauncherPreview from '../components/InteractiveLauncherPreview'

<InteractiveLauncherPreview
  isExpanded={isPreviewExpanded}
  isMinimized={isPreviewMinimized}
  isClosed={isPreviewClosed}
  onExpand={() => setIsPreviewExpanded(!isPreviewExpanded)}
  onMinimize={() => setIsPreviewMinimized(!isPreviewMinimized)}
  onClose={() => setIsPreviewClosed(true)}
>
  {/* Содержимое превью лаунчера */}
</InteractiveLauncherPreview>
```

### Состояния

- **Развёрнуто** (`isExpanded: true`): Полноразмерное окно с 3D эффектами
- **Свёрнуто** (`isMinimized: true`): Компактный режим с возможностью перетаскивания
- **Закрыто** (`isClosed: true`): Окно скрыто

## Производительность

### Оптимизации

1. **Использование `will-change`**: Предупреждает браузер о предстоящих изменениях
2. **`requestAnimationFrame`**: Синхронизация с частотой обновления экрана
3. **Ограничение частиц**: Максимум 60 частиц одновременно
4. **Автоочистка**: Удаление завершённых анимаций из DOM

### Рекомендации

- Эффекты оптимизированы для 60 FPS
- Используют GPU-ускорение через `transform` и `opacity`
- Минимальное влияние на производительность страницы

## Кастомизация

### Изменение цветов

В файле `InteractiveLauncherPreview.css`:

```css
/* Цвет частиц */
.particle {
  background: linear-gradient(135deg, #8A4BFF, #FF6B9D);
}

/* Цвет свечения */
.hover-glow {
  background: radial-gradient(circle at center, rgba(138, 75, 255, 0.15) 0%, transparent 70%);
}
```

### Изменение физики

В файле `InteractiveLauncherPreview.tsx`:

```tsx
// Затухание инерции
x: prev.x * 0.92  // Увеличьте для более долгого движения

// Возврат к центру
const newX = prev.x * 0.85  // Уменьшите для более медленного возврата

// Гравитация частиц
vy: p.vy + 0.5  // Увеличьте для более быстрого падения
```

## Совместимость

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

## Новая функция: Перетаскиваемое превью GUI (Launcher HomePage)

### 8. 🎮 Draggable GUI Preview with KDE Plasma Jelly Effect

**Описание:**
На главной странице лаунчера (HomePage) добавлено интерактивное превью GUI, которое можно свободно перетаскивать по экрану с эффектом желе, как в KDE Plasma.

**Как работает:**
- Превью отображается в начале страницы, когда лаунчер не в процессе установки или запуска
- Можно захватить мышью и перетащить в любое место экрана
- При перетаскивании применяется физика пружины с эффектом желе
- После отпускания элемент продолжает двигаться с инерцией и затуханием
- Визуальные эффекты: деформация формы, вращение иконки, эффект ряби

**Технические детали:**
- Физика пружины: жесткость 0.05, затухание 0.85
- Позиция в процентах относительно родительского контейнера
- Анимация через `requestAnimationFrame` для 60 FPS
- Автоматическое скрытие во время установки/запуска

**Параметры эффекта желе:**
- Деформация границ: 22px-26px (волнообразно)
- Масштабирование: 0.95-1.05 (пульсация)
- Вращение иконки: ±2 градуса
- Эффект ряби при перетаскивании

**Визуальные состояния:**
- **Покой**: Плавающая анимация, мягкое свечение
- **Наведение**: Изменение курсора на grab
- **Перетаскивание**: 
  - Курсор grabbing
  - Увеличение масштаба на 5%
  - Вращение иконки
  - Эффект ряби вокруг элемента
  - Усиленное свечение

**Код интеграции:**
```tsx
// В HomePage.tsx автоматически отображается
{!isLaunching && !isInstalling && (
  <div className="gui-preview" /* ... */ >
    {/* Превью GUI */}
  </div>
)}
```

## Будущие улучшения

- [ ] Звуковые эффекты при взаимодействии
- [ ] Haptic feedback для мобильных устройств
- [ ] Дополнительные темы эффектов
- [ ] Настройки интенсивности эффектов
- [ ] Режим энергосбережения
- [x] Перетаскиваемое превью GUI с эффектом желе (KDE Plasma style)

## Примеры использования

### Минимальная конфигурация

```tsx
<InteractiveLauncherPreview
  isExpanded={false}
  isMinimized={false}
  isClosed={false}
  onExpand={() => {}}
  onMinimize={() => {}}
  onClose={() => {}}
>
  <div>Контент</div>
</InteractiveLauncherPreview>
```

### Полная конфигурация с состоянием

```tsx
const [state, setState] = useState({
  expanded: false,
  minimized: false,
  closed: false
})

<InteractiveLauncherPreview
  isExpanded={state.expanded}
  isMinimized={state.minimized}
  isClosed={state.closed}
  onExpand={() => setState(s => ({ ...s, expanded: !s.expanded }))}
  onMinimize={() => setState(s => ({ ...s, minimized: !s.minimized }))}
  onClose={() => setState(s => ({ ...s, closed: true }))}
>
  <LauncherContent />
</InteractiveLauncherPreview>
```

## Отладка

### Включение визуализации физики

Добавьте в компонент:

```tsx
// Показать границы частиц
.particle {
  border: 1px solid red;
}

// Показать область ripple
.ripple {
  border: 1px solid blue;
}
```

### Логирование событий

```tsx
const handleMouseDown = (e: React.MouseEvent) => {
  console.log('Drag started:', e.clientX, e.clientY)
  // ...
}
```

## Поддержка

Если у вас возникли вопросы или предложения по улучшению интерактивных эффектов, создайте issue в репозитории проекта.


# Перетаскиваемое превью GUI с эффектом желе

## 🎮 Описание

На главной странице лаунчера добавлено интерактивное превью GUI, которое можно свободно перетаскивать по экрану с эффектом желе, как в KDE Plasma.

## ✨ Возможности

### Основные функции
- 🖱️ **Перетаскивание мышью** - захватите и перемещайте превью в любое место
- 🌊 **Эффект желе** - реалистичная деформация при движении (KDE Plasma style)
- 💫 **Физика пружины** - плавное затухание и инерция после отпускания
- 🎨 **Визуальные эффекты** - свечение, рябь и вращение при перетаскивании

### Визуальные состояния

#### Покой
- Плавающая анимация
- Мягкое фиолетовое свечение
- Текст "Перетащи меня!"

#### Перетаскивание
- Деформация формы (эффект желе)
- Вращение в направлении движения
- Эффект ряби вокруг элемента
- Усиленное свечение
- Вращающаяся иконка

## 🔧 Технические детали

### Физика
```typescript
// Параметры пружины
springStrength: 0.05    // Жесткость возврата
damping: 0.88          // Затухание движения

// Деформация
maxDeform: 0.25        // Максимальная деформация (25%)
rotationFactor: 0.8    // Коэффициент вращения
```

### Производительность
- ✅ 60 FPS через `requestAnimationFrame`
- ✅ GPU-ускорение (`transform`, `will-change`)
- ✅ Автоматическая очистка анимаций
- ✅ Оптимизированные вычисления

## 📝 Использование

### Автоматическое отображение
Превью автоматически появляется на главной странице, когда:
- ✅ Лаунчер не в процессе установки
- ✅ Лаунчер не в процессе запуска

### Взаимодействие
1. **Наведите** курсор на превью (курсор изменится на 🖐️)
2. **Нажмите** левую кнопку мыши и удерживайте
3. **Перетащите** превью в нужное место
4. **Отпустите** кнопку мыши - превью продолжит движение с инерцией

## 🎨 Кастомизация

### Изменение размера
```css
/* В HomePage.css */
.gui-preview {
  width: 120px;   /* Ширина */
  height: 120px;  /* Высота */
}
```

### Изменение цветов
```css
/* Фон превью */
.gui-preview-content {
  background: rgba(138, 75, 255, 0.15);
  border-color: rgba(138, 75, 255, 0.4);
}

/* Эффект свечения */
.gui-preview.dragging::before {
  background: radial-gradient(circle, rgba(138, 75, 255, 0.4) 0%, transparent 70%);
}
```

### Изменение физики
```typescript
// В HomePage.tsx

// Более сильное затухание (быстрее останавливается)
const damping = 0.95

// Более слабое затухание (дольше движется)
const damping = 0.80

// Более сильная деформация
const deformAmount = Math.min(speed * 0.5, 0.35)

// Более слабая деформация
const deformAmount = Math.min(speed * 0.1, 0.15)
```

## 🐛 Отладка

### Визуализация границ
```css
.gui-preview {
  outline: 2px solid red; /* Показать границы */
}
```

### Логирование событий
```typescript
const handleMouseDown = (e: React.MouseEvent) => {
  console.log('🖱️ Начало перетаскивания:', { x: e.clientX, y: e.clientY })
  // ...
}

const handleMouseMove = (e: MouseEvent) => {
  console.log('📍 Позиция:', position)
  console.log('⚡ Скорость:', velocity)
  // ...
}
```

## 🌐 Совместимость

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Electron (текущая версия)

## 📚 Связанные файлы

- `Launcher/src/pages/HomePage.tsx` - Логика перетаскивания
- `Launcher/src/styles/HomePage.css` - Стили и анимации
- `docs/interactive-features.md` - Полная документация

## 🚀 Будущие улучшения

- [ ] Сохранение позиции в localStorage
- [ ] Магнитное прилипание к краям
- [ ] Звуковые эффекты
- [ ] Настройки интенсивности эффектов
- [ ] Мобильная версия (touch events)

## 💡 Вдохновение

Эффект желе вдохновлен знаменитым эффектом "wobbly windows" из KDE Plasma, который создает ощущение эластичности и живости интерфейса.

---

**Приятного использования! 🎉**
