# Сводка изменений: JWT токены

## Проблема:
При передеплое сервера на Render.com пользователи выходили из аккаунта, потому что сессии хранились в памяти сервера.

## Решение:
Переход с серверных сессий на JWT токены, которые хранятся на клиенте.

---

## Измененные файлы:

### Сервер:
1. **server/index.js** - полностью переписан с JWT токенами
   - Убрана зависимость от `express-session`
   - Добавлена генерация JWT токенов (срок: 30 дней)
   - Passport настроен без сессий
   - Токен передается клиенту при успешной аутентификации

2. **server/index.backup.js** - резервная копия старой версии

### Клиент (веб):
3. **src/utils/database.ts**
   - Токен и пользователь сохраняются в `localStorage`
   - Добавлена функция `getAuthToken()`
   - При выходе токен удаляется

4. **src/pages/AuthPage.tsx**
   - Обновлена обработка OAuth callback
   - Токен автоматически сохраняется при входе

### Клиент (лаунчер):
5. **Launcher/src/App.tsx**
   - Токен сохраняется отдельно от данных пользователя
   - При выходе токен удаляется

### Конфигурация:
6. **.env**
   - Добавлена переменная `JWT_SECRET`

7. **package.json**
   - Добавлена зависимость `jsonwebtoken: ^9.0.2`

---

## Новые файлы:

1. **JWT_MIGRATION_README.md** - техническая документация миграции
2. **RENDER_DEPLOY_INSTRUCTIONS.md** - инструкция по деплою на Render
3. **ОБНОВЛЕНИЕ_JWT.md** - краткая инструкция на русском
4. **CHECKLIST.md** - чеклист перед деплоем
5. **deploy.bat** - скрипт для быстрого деплоя
6. **SUMMARY.md** - этот файл

---

## Как это работает:

### До обновления:
```
Пользователь → Вход → Сессия на сервере → Передеплой → Сессия потеряна → Выход
```

### После обновления:
```
Пользователь → Вход → JWT токен на клиенте → Передеплой → Токен остается → Авторизован
```

---

## Преимущества:

✅ **Устойчивость к передеплою** - пользователи не выходят  
✅ **Масштабируемость** - сервер не хранит состояние  
✅ **Безопасность** - токены подписаны и имеют срок действия  
✅ **Удобство** - авторизация сохраняется между сессиями  

---

## Что нужно сделать:

1. ✅ Код готов и проверен
2. ⏳ Добавить `JWT_SECRET` на Render.com
3. ⏳ Задеплоить на Render
4. ⏳ Проверить работу

---

## Команды для деплоя:

```bash
# Вариант 1: Использовать скрипт
deploy.bat

# Вариант 2: Вручную
git add .
git commit -m "Миграция на JWT токены"
git push origin main
```

---

## Проверка после деплоя:

1. Открыть https://oneshakedown.onrender.com
2. Войти через Google/Yandex
3. Закрыть браузер
4. Открыть снова - должны остаться авторизованными
5. Сделать Manual Deploy на Render
6. Проверить, что не вышло из аккаунта

---

## Важно:

⚠️ Существующие пользователи должны войти заново **один раз**  
⚠️ Не забудьте добавить `JWT_SECRET` на Render.com  
⚠️ Резервная копия: `server/index.backup.js`  

---

## Технические детали:

- **JWT библиотека**: jsonwebtoken v9.0.2
- **Срок действия токена**: 30 дней
- **Алгоритм**: HS256 (HMAC SHA-256)
- **Хранилище**: localStorage (ключи: `shakedown_user`, `shakedown_token`)
- **Секрет**: `JWT_SECRET` из переменных окружения

---

Готово! 🚀
